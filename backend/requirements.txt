# ============================================================================
# Stock Screening Platform - Backend Dependencies
# ============================================================================

# Web Framework
fastapi==0.121.1  # Security: Latest stable, supports starlette 0.47.2
starlette==0.47.2  # Security: Fixed DoS vulnerabilities (GHSA-2c2j-9gv5-cj73, GHSA-7f5h-v6xp-fcq8)
uvicorn[standard]==0.24.0
gunicorn==22.0.0  # Security: Fixed request smuggling (GHSA-w3h3-4rj7-4ph4, GHSA-hc5x-x2vx-497g)

# Database
sqlalchemy[asyncio]==2.0.23
asyncpg==0.29.0
alembic==1.12.1
psycopg2-binary==2.9.9

# Cache
redis[hiredis]==5.0.1

# Authentication & Security
python-jose[cryptography]==3.4.0  # Security: Fixed algorithm confusion and JWT bomb (PYSEC-2024-232, PYSEC-2024-233)
bcrypt==4.1.2
python-multipart==0.0.20  # Security: Fixed ReDoS vulnerabilities (GHSA-2jv5-9r88-3w3p, GHSA-59g5-xgcq-4qw3)
pydantic[email]==2.5.0
pydantic-settings==2.1.0

# Background Tasks
# Note: Celery 5.4.0+ supports redis 5.0+
celery[redis]==5.4.0

# Data Processing
pandas==2.1.3
numpy==1.26.2

# HTTP Requests
httpx==0.25.2
aiohttp==3.11.14  # Security: Fixed directory traversal, request smuggling, XSS, DoS (6 CVEs)

# Monitoring & Logging
prometheus-client==0.19.0
sentry-sdk[fastapi]==2.22.0  # Security: Fixed env variable exposure (GHSA-g92j-qhmh-64v2)

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
aiosqlite==0.19.0
# httpx already listed in HTTP Requests section

# Code Quality
black==24.10.0  # Security: Fixed ReDoS (PYSEC-2024-48)
flake8==6.1.0
mypy==1.7.1
isort==5.13.0

# Utilities
python-dotenv==1.0.0
pyyaml==6.0.1
