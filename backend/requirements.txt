# ============================================================================
# Stock Screening Platform - Core Backend Dependencies
# ============================================================================
# For ML/AI training and serving dependencies, see requirements-ml.txt

# Web Framework
fastapi==0.139.2  # Security: Latest stable
starlette==1.3.1  # Security: Fixed CVE-2026-48817/48818, PYSEC-2026-248/249 + prior BadHost and HTTP Range DoS CVEs
uvicorn[standard]==0.42.0
gunicorn==25.3.0  # Security: Fixed request smuggling (GHSA-w3h3-4rj7-4ph4, GHSA-hc5x-x2vx-497g)

# Database
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0
alembic==1.18.4
psycopg2-binary==2.9.11
greenlet==3.3.2  # Required for SQLAlchemy asyncio

# Cache
redis[hiredis]==5.3.1

# Authentication & Security
python-jose[cryptography]==3.5.0  # Security: Fixed algorithm confusion and JWT bomb (PYSEC-2024-232, PYSEC-2024-233)
bcrypt==5.0.0
python-multipart==0.0.31  # Security: Fixed CVE-2026-53538/53539/53540 + prior multipart DoS/ReDoS CVEs
pydantic[email]==2.13.4
pydantic-settings==2.14.2  # Security: Fixed GHSA-4xgf-cpjx-pc3j

# Payment Processing
stripe==15.0.1

# Background Tasks
# Note: Celery 5.4.0+ supports redis 5.0+
celery[redis]==5.6.3

# LLM API Clients (lightweight, used by core AI analysis endpoints)
openai==2.30.0  # Upgraded: httpx 0.28.x compatibility (fixes proxies parameter issue)
anthropic==0.89.0  # Security: Fixed CVE-2026-34450, CVE-2026-34452
tenacity==9.1.4
jinja2==3.1.6
pywebpush==2.3.0

# HTTP Requests
httpx==0.28.1
aiohttp==3.14.1  # Security: Fixed PYSEC-2026-237 and CVE-2026-34993/47265/50269/54273~54280

# Monitoring & Logging
prometheus-client==0.24.1
sentry-sdk[fastapi]==2.63.0  # Security: Fixed env variable exposure (GHSA-g92j-qhmh-64v2)

# Testing
pytest==9.1.1  # Security: Fixed CVE-2025-71176 (insecure /tmp/pytest-of-{user} dir handling)
pytest-asyncio==1.3.0
pytest-cov==7.1.0
aiosqlite==0.22.1
freezegun==1.5.5
# httpx already listed in HTTP Requests section

# Code Quality
black==26.5.1  # Security: Fixed ReDoS (PYSEC-2024-48)
flake8==7.3.0
mypy==1.20.0
isort==8.0.1

# Utilities
python-dotenv==1.2.2  # Upgraded: Required by mlflow 3.x (fastmcp dependency)
pyyaml==6.0.3
email-validator==2.3.0
zipp>=3.19.1
tornado>=6.5.7
idna>=3.18
certifi>=2024.07.04
