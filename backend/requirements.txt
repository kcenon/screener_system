# ============================================================================
# Stock Screening Platform - Core Backend Dependencies
# ============================================================================
# For ML/AI training and serving dependencies, see requirements-ml.txt

# Web Framework
fastapi==0.135.2  # Security: Latest stable, supports starlette 0.49.1
starlette==1.0.0  # Security: Fixed HTTP Range DoS (GHSA-7f5h-v6xp-fcq8, CVE-2025-62727)
uvicorn[standard]==0.42.0
gunicorn==25.3.0  # Security: Fixed request smuggling (GHSA-w3h3-4rj7-4ph4, GHSA-hc5x-x2vx-497g)

# Database
sqlalchemy[asyncio]==2.0.48
asyncpg==0.31.0
alembic==1.18.4
psycopg2-binary==2.9.11
greenlet==3.3.2  # Required for SQLAlchemy asyncio

# Cache
redis[hiredis]==5.3.1

# Authentication & Security
python-jose[cryptography]==3.5.0  # Security: Fixed algorithm confusion and JWT bomb (PYSEC-2024-232, PYSEC-2024-233)
bcrypt==5.0.0
python-multipart==0.0.22  # Security: Fixed ReDoS vulnerabilities (GHSA-2jv5-9r88-3w3p, GHSA-59g5-xgcq-4qw3)
pydantic[email]==2.12.5
pydantic-settings==2.13.1  # Upgraded: Compatible with pydantic 2.11.7

# Payment Processing
stripe==15.0.0

# Background Tasks
# Note: Celery 5.4.0+ supports redis 5.0+
celery[redis]==5.6.2

# LLM API Clients (lightweight, used by core AI analysis endpoints)
openai==2.30.0  # Upgraded: httpx 0.28.x compatibility (fixes proxies parameter issue)
anthropic==0.89.0  # Security: Fixed CVE-2026-34450, CVE-2026-34452
tenacity==9.1.4
jinja2==3.1.6
pywebpush==2.3.0

# HTTP Requests
httpx==0.28.1
aiohttp==3.13.5  # Security: Fixed CVE-2026-34515~34519 and prior CVEs

# Monitoring & Logging
prometheus-client==0.24.1
sentry-sdk[fastapi]==2.56.0  # Security: Fixed env variable exposure (GHSA-g92j-qhmh-64v2)

# Testing
pytest==9.0.2
pytest-asyncio==1.3.0
pytest-cov==7.1.0
aiosqlite==0.22.1
freezegun==1.5.5
# httpx already listed in HTTP Requests section

# Code Quality
black==26.3.1  # Security: Fixed ReDoS (PYSEC-2024-48)
flake8==7.3.0
mypy==1.19.1
isort==8.0.1

# Utilities
python-dotenv==1.2.2  # Upgraded: Required by mlflow 3.x (fastmcp dependency)
pyyaml==6.0.3
email-validator==2.3.0
zipp>=3.19.1
tornado>=6.4.1
idna>=3.7
certifi>=2024.07.04
