# Backend upstream — managed by deploy-bluegreen.sh during blue-green deployments.
# For standard deployments, this file is static.
upstream backend_api {
    least_conn;
    server backend:8000 max_fails=3 fail_timeout=30s;
    keepalive 32;
}
