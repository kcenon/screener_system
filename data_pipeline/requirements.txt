# Apache Airflow and Providers
# =============================================================================

# Core Airflow with constraints for Python 3.11
# Airflow 3.2.2 fixes werkzeug, flask-appbuilder, and Airflow 3.2.0 CVEs
apache-airflow[postgres,celery]==3.2.2  # Security: Fixed Airflow 3.2.0 CVEs and prior werkzeug/flask-appbuilder CVEs

# Security: Override Airflow's transitive starlette dependency
# Airflow pulls vulnerable starlette releases unless pinned above the fixed range
starlette==1.3.1  # Security: Fixed CVE-2026-48817/48818, PYSEC-2026-161/248/249

# Database Drivers
# =============================================================================
asyncpg==0.31.0  # Async PostgreSQL driver
psycopg2-binary==2.9.12  # PostgreSQL driver (for Airflow metadata DB)

# Data Processing
# =============================================================================
pandas==3.0.2
numpy==2.4.4

# HTTP Client
# =============================================================================
requests==2.34.2
httpx==0.28.1  # Async HTTP client

# Cache
# =============================================================================
redis==8.0.1  # Redis client for caching

# Utilities
# =============================================================================
python-dotenv==1.2.2  # Environment variable management

# Testing
# =============================================================================
pytest==9.1.1
pytest-asyncio==1.4.0
